import React from 'react';

export default function ColorPickButton(props) {
    const style = {
        padding: 10,
        margin: 20,
        display: 'block',
        backgroundColor: props.color,
        borderRadius: '50%',
        width: 30,
        height: 30,
    };
    return <div></div>;
}
